# Archived Repository Notes

This directory contains historical migration notes, cleanup summaries, and one-off progress documents that were previously stored in the repository root.

They are kept for project history, but they are not the main entry points for new contributors.

Start here instead:
- `README.md` for the repository overview
- `CONTRIBUTING.md` for contribution workflow
- `ruststream-core/README.md` for crate-level usage
